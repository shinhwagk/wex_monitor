package modles.common

import java.text.SimpleDateFormat
import java.util.Date

import play.api.libs.json.{JsValue, Json, Reads, Writes}

/**
  * Created by zhangxu on 2016/8/3.
  */
object JsonCommon {




  //
  //  def ListStringToJsonString(ls: List[String]): JsValue = {
  //    Json.toJson(ls.map(Json.parse(_)))
  //  }

  //  def transformTimestampToDate(ls: List[String]) = {
  //    ls.map(jsonToObject[NodeInfo](_)).map(jv => new Date(jv.timestamp.toLong).toString)
  //  }

}

